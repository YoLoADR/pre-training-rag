# ─────────────────────────────────────────────────────────────────────────
# Atelier 03 — Pipeline RAG + Agent ReAct (J2 matin)
# Scope : atelier 02 + ChromaDB + EnsembleRetriever + agent LangChain + Gradio.
# ─────────────────────────────────────────────────────────────────────────

# ── atelier 02 (LLM + RAG FAISS) ──────────────────────────────────────────
python-dotenv==1.0.1
pydantic>=2.7.4
pydantic-settings>=2.3.4
langchain==0.3.14
langchain-core==0.3.41
langchain-community==0.3.14
langchain-anthropic==0.3.0
anthropic==0.40.0
ollama==0.4.4
onnxruntime==1.23.2
fastembed>=0.8.0
faiss-cpu==1.13.2
pymupdf==1.25.1
fpdf2==2.8.1
pandas>=2.2.3
numpy>=2.0.0
jupyter==1.1.1
ipykernel==6.29.5

# ── ChromaDB (FAQ dynamiques, filtres métadonnées) ────────────────────────
chromadb==0.5.23

# ── Chunking sémantique (SemanticChunker) ─────────────────────────────────
langchain-experimental==0.3.4

# ── Services externes (Open-Meteo, marketplace) ───────────────────────────
requests==2.32.3
cachetools==5.5.1

# ── UI prototype agent ────────────────────────────────────────────────────
gradio==5.9.1
