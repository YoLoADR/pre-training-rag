# ─────────────────────────────────────────────────────────────────────────
# Atelier 02 — RAG Simple (J1 après-midi)
# Scope : atelier 01 + ingestion PDF + embeddings + FAISS (pas de ChromaDB).
# ─────────────────────────────────────────────────────────────────────────

# ── atelier 01 (LLM) ──────────────────────────────────────────────────────
python-dotenv==1.0.1
pydantic>=2.7.4
pydantic-settings>=2.3.4
langchain==0.3.14
langchain-core==0.3.41
langchain-community==0.3.14
langchain-anthropic==0.3.0
anthropic==0.40.0
ollama==0.4.4

# ── RAG (FAISS only) ──────────────────────────────────────────────────────
# onnxruntime: 1.23.2 = dernière version avec wheels macOS x86_64 (Mac Intel).
onnxruntime==1.23.2
fastembed>=0.8.0
faiss-cpu==1.13.2

# ── PDF parsing & data ────────────────────────────────────────────────────
pymupdf==1.25.1       # s'importe `import fitz`
fpdf2==2.8.1
pandas>=2.2.3
numpy>=2.0.0

# ── Notebooks ─────────────────────────────────────────────────────────────
jupyter==1.1.1
ipykernel==6.29.5
