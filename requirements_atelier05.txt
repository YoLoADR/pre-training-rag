# ─────────────────────────────────────────────────────────────────────────
# Atelier 05 — Déploiement + Supervision (J3 matin)
# Scope : atelier 04 + FastAPI + Streamlit + Langfuse + rate limiting.
# ─────────────────────────────────────────────────────────────────────────

# ── atelier 04 (= atelier 03 + dataset FT) ────────────────────────────────
-r requirements_atelier04.txt

# ── API REST + sécurité ───────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.27.2
python-multipart>=0.0.18
slowapi>=0.1.0

# ── UI Streamlit (dashboard 4 pages) ──────────────────────────────────────
streamlit==1.41.1
plotly==5.24.1

# ── Observabilité (traces LLM) ────────────────────────────────────────────
langsmith==0.2.3
langfuse==2.57.1
